# Copy this file to .env and fill in real values.
# .env is gitignored — never commit real keys.
# IMPORTANT: rotate the two keys that were previously committed in
# README_RUN_ORDER.md (Web of Science + Scopus) before sharing this repo.

GROQ_API_KEY=
SCOPUS_API_KEY=
WOS_API_KEY=
